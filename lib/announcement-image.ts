/** Preferred announcement banner size — optional, for uniform feed layout. */
export const ANNOUNCEMENT_IMAGE_PREFERRED = {
  width: 1200,
  height: 675,
  label: "1200 × 675 px (16:9)",
} as const;
